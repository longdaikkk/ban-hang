// JSS
// css in js

import styled from "styled-components";

export const WrapperSpin = styled.div`
        width: 100%;
        height: ${props => props.viewHeigh};
        justify-content: center;
        display: flex;
        align-items: center;
        position: absolute;
        z-index: 1;
        background: #ffffff63;
        `